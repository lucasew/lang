# attic

Pre–1:1 product-shaped code moved out of the live tree.

Do **not** extend this as the LanguageTool port. New work goes under
`internal/languagetool/` matching Java packages.

Pieces may return only after an explicit 1:1 check against upstream LanguageTool.
